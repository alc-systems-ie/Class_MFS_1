# Zephyr / west build output. Note build-dongle/ (the tools/toggle_dongle build)
# and any other build* directory, not just build/.
build*/
compile_commands.json
.cache/

# Secrets — TAN seed and provisioning key. Never commit.
credentials.conf

# macOS.
.DS_Store
